{
  "experimentalServices": {
    "agentcred": {
      "type": "web",
      "routePrefix": "/",
      "framework": "nextjs",
      "buildCommand": "npm ci --include=dev && npx prisma generate && npx prisma db push && npm run build"
    }
  }
}
